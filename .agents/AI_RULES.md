# AI Output Rules (Repository-wide)

- Output production-ready code.
- Include full file paths.
- Minimal prose; explain *why* in inline comments only when needed.
- Prefer patches / diffs (only changed blocks) when possible.
- If requirements are ambiguous: ask a short question before coding.
- Before writing dates into docs: run `date` and use the real current date.
