{% for item in items %}
  <div class="date">
    <span class="day">{{ item.content[0]['#markup']|date('j') }}</span>
    <span class="year">{{ item.content[0]['#markup']|date('M.Y') }}</span>
  </div>
{% endfor %}
